{
  "name": "black-jack",
  "version": "1.0.0",
  "description": "A game of Black Jack to try out testing.",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "nyc --all --report-dir build/coverage --temp-directory build/.nyc_output --reporter=html --reporter=text mocha 'test/**/*.js'",
    "report-coveralls": "nyc report --reporter=text-lcov | coveralls",
    "report-codecov": "nyc report --reporter=lcov > coverage.lcov && codecov"
  },
  "author": "Mikael Roos",
  "license": "MIT",
  "devDependencies": {
    "clean-css-cli": "^4.1.10",
    "codecov": "^3.0.0",
    "coveralls": "^3.0.0",
    "csslint": "^1.0.5",
    "eslint": "^4.9.0",
    "eslint-plugin-react": "^7.4.0",
    "html-minifier": "^3.5.6",
    "htmlhint": "^0.9.13",
    "js-yaml": "^3.10.0",
    "jscs": "^3.0.7",
    "jsonlint": "^1.6.2",
    "mocha": "^4.0.1",
    "nyc": "^11.2.1",
    "stylelint": "^8.2.0",
    "uglify-js": "^3.1.5"
  }
}
